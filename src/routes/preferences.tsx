const Preferences = () => {
    // todo 记录应用的设置信息
    // 根据不同的类型分段显示
    // 使用两个组件 一个是PreferenceSegment 一个是其中的Preference
    // PreferenceSegment有label负责将同一个类型的Preference联系起来
    // Preference负责通过KEY将后端的配置信息拿到前端来进行展示，且支持修改
    return (
        <div>

        </div>
    );
};

export default Preferences;
